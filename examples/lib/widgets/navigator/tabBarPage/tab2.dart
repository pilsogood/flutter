import "package:flutter/material.dart";

class tab2 extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text('Tab2 Page'),
      )
    );
  }
}